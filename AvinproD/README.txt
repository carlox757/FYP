To access the database one has to be inside KU network as database is hosted by them and do not allow thrid party programs outside the network to connect.

The executable is found in AvinproD>>out>>Artifacts>>AvinproD_jar